---
layout: post
title: 权限问题
date: 2026-08-13
tags: 权限 旁注 目录越权 跨库查询 CDN 越权
description: 权限安全笔记：旁站查询与入侵、目录越权、SQL 跨库查询、CDN 识别与绕过、越权漏洞分类与 pikachu 实战演示。
---

# 权限问题

## 旁注

在同一服务器上有多个站点，我们要攻击的这个站点假设没有漏洞，我们可以攻击服务器上的任意一个站点，这个就是旁注。

### 旁站查询

也就是同一个 IP 地址下的多个域名，查询方式：可通过 ping 域名获取其相关 IP 地址，之后通过 IP 地址反查获取其旁注的域名。

```
http://tool.chinaz.com/Same/
http://dns.aizhan.com/
http://www.11best.com/ip/
```

### 旁站入侵

因为你通过木马程序入侵了一个网站之后，你用的用户是 `IUSR` 用户，而其他网站大家也都是 `IUSR` 用户，权限都是一样的，所以你能够操作其他网站的目录。

### 防范

防止旁站入侵我们就是通过给每个网站设置一个单独的账号，每个账号单独的密码，这样就能防范旁站入侵。

> ⚠️ 注意不要用管理员账号，不然很危险。

添加几个新用户，比如给他们都划分到 Guests 组（普通用户组），然后将每个网站给一个单独的用户权限。

步骤：

1. 我的电脑 → 管理 → 本地用户和组 → 用户 → 创建用户 `test1`

![image-20260730225707597](/assets/images/20/image-20260730225707597.png)

2. 组 → Guests → 添加到组 → 添加 → 搜索 `test1`

![image-20260730225939961](/assets/images/20/image-20260730225939961.png)

3. 找到 IIS → 网站 → 属性 → 目录安全性 → 编辑 → 浏览 → 找到 `test1`

### 旁站工具

> 💡 工具存放路径（win10）：`C:\信息收集\旁站`

#### IISPutScanner

![image-20260730231015743](/assets/images/20/image-20260730231015743.png)

#### Goby

![image-20260730231406686](/assets/images/20/image-20260730231406686.png)

---

## 目录越权

运维人员使用了同一个 web 服务应用程序（中间件）用户而造成目录越权，同时中间件用户权限过高也可以造成目录越权。这个和我们刚才说的旁注产生的原因一样。很多运维人员都喜欢用 root 用户来搭建 web 服务，这种情况下如果别人拉取到 webshell，那么就很容易控制你的服务器。

目录越权就是你这个用户访问了你本来没有权限访问的目录。

IIS → 网站 → 属性 → 主目录

![image-20260730233457063](/assets/images/20/image-20260730233457063.png)

这个 `XYCMS` 目录才是你可以访问的，但是你却又可以访问别的路径下的。

### 防范

> 💡 Web 服务进程不要以 root 运行：即便出现目录遍历，也读不到系统敏感文件。

---

## sql跨库查询

跨库查询是指由于权限设置不严格，导致普通帐号被授予过高的权限，从而使得其可以对其他的数据库进行操作。比如，在 MySQL 中，`information_schema` 这个表默认只有 root 有权限进行操作。但是如果一个普通账户权限过高后，他便可以对该数据库进行操作，从而影响整个 MySQL 数据库的运行。其实主要也是因为运维人员或者 DBA 的安全意识不够导致的。

### 防范

我们应该在数据库创建一个普通用户，然后供给开发人员等使用。

SQL 语法：

```sql
GRANT 权限列表 ON 库.表 TO '用户名'@'访问主机' [IDENTIFIED BY '密码'];
-- 权限列表：SELECT / INSERT / UPDATE / DELETE 等，多个权限用逗号分隔
-- 库.表：指定授权的数据库和表，*.* 表示所有库所有表
-- '用户名'@'访问主机'：允许哪个用户从哪个主机连接，localhost 表示仅本机
-- IDENTIFIED BY：设置该账号的密码（可省略）
```

| 普通权限 | 功能说明     |
| -------- | ------------ |
| SELECT   | 查询表数据   |
| INSERT   | 新增数据     |
| UPDATE   | 修改已有数据 |
| DELETE   | 删除数据记录 |

| 高危权限                | 危害                                         |
| ----------------------- | -------------------------------------------- |
| FILE                    | 读写服务器本地任意文件（读取配置、写入木马） |
| SUPER                   | 超级管理权限，可修改系统参数、关停数据库     |
| GRANT OPTION            | 可将自身权限分给其他用户                     |
| CREATE USER / DROP USER | 新建、删除数据库账号                         |

---

## CDN

把网站图片、视频、安装包、静态文件，提前缓存到全国各地节点服务器，用户就近调取资源，不用远距离访问源站，实现提速 + 减负。

### 如何判断cdn

#### 系统的nslookup指令

示例：

```cmd
:: 查询域名解析结果，出现别名（CNAME）记录说明大概率挂了 CDN
nslookup www.163.com
```

![image-20260731071636917](/assets/images/20/image-20260731071636917.png)

> 💡 别名调度就是 CDN 厂商实现就近节点分配的标准方式。

### 多ping检测

```
http://ping.chinaz.com/
http://ping.aizhan.com/
http://ce.cloud.360.cn/
```

在不同的地方 ping 一个 IP 地址，如果存在 CDN 服务器，那么不同地方的解析记录应该不同。

![image-20260731072838780](/assets/images/20/image-20260731072838780.png)

![image-20260731072855405](/assets/images/20/image-20260731072855405.png)

### cdn绕过

> 🎯 不经过 CDN 服务器，直接去访问你的服务器，去找到你真实的 IP 地址。

之前信息收集博客里面提到过：[information_gathering](https://fan-seclab.github.io/2026/04/information_gathering/) 里面的收集真实 IP。

补充：

#### 网站漏洞查找

1）目标敏感文件泄露，例如：phpinfo 之类的探针、`info.php`、`phpinfo.php`、`test.php`、`l.php`、GitHub 信息泄露等。

2）查看漏洞扫描报警信息，手工造成页面报错。

3）XSS 盲打，命令执行反弹 shell，SSRF 等。

4）无论是用社工还是其他手段，拿到了目标网站管理员在 CDN 的账号，从而在从 CDN 的配置中找到网站的真实 IP，这个太难了，如果要做的话，一般是钓鱼的方式，比如发邮件之类的。

5）直接 ping 一个 `asdfasdf.baidu.com`，如果开启了泛域名解析，那么 ping 到的一般是真实 IP 地址。

#### 用 Zmap 扫全网

就是扫描整个网段，比如就扫描 80、443 端口，每个网站不是都有标题标签码，以关键字的方式来匹配提取，不过这个方法太麻烦了。

#### DDOS把CDN流量打光

CDN 是收费的，花钱买流量，DDOS 把流量打光了，人家就不给你提供服务了，真实 IP 地址就暴露出来了。

---

## 越权

所谓越权，就是绕过权限限制或者在权限限制不够严格的情况下去访问原来不应该访问到的资源。比如：目录越权、任意订单遍历的业务类越权等等。其中，目录越权分为多种，常见的有：目录权限设置不严格、权限没有递归设置、web 站点越权访问、用户权限鉴权被绕过。

### 原理

如果使用 A 用户的权限去操作 B 用户的数据，A 的权限小于 B 的权限，如果能够成功操作，则称之为越权操作。越权漏洞形成的原因是后台使用了不合理的权限校验规则导致的。

### 如何挖掘越权漏洞

简单来说就是挖掘那种普通用户操作管理员权限的功能。

### 越权分类

| 类型       | 场景                                         |
| ---------- | -------------------------------------------- |
| 水平越权   | A、B 用户同级，A 越权操作 B 的个人信息       |
| 垂直越权   | B 用户权限低于 A 用户，B 越权操作 A 的权限   |
| 未授权访问 | 没有登录的情况下，访问了只有登录才能看到的页面 |

#### 水平越权

A 用户和 B 用户属于同一级别用户，但各自不能操作对方个人信息，A 用户如果越权操作 B 用户的个人信息的情况称之为平行越权操作。

pikachu 靶场

Over Permission

lucy

![image-20260731101356824](/assets/images/20/image-20260731101356824.png)

lili

![image-20260731101535335](/assets/images/20/image-20260731101535335.png)

URL：

```http
http://192.168.239.131/pikachu/vul/overpermission/op1/op1_mem.php?username=lili&submit=%E7%82%B9%E5%87%BB%E6%9F%A5%E7%9C%8B%E4%B8%AA%E4%BA%BA%E4%BF%A1%E6%81%AF
```

我们把 `username` 改成 `lucy`，发现页面变成了 lucy 的身份信息。

![image-20260731101654812](/assets/images/20/image-20260731101654812.png)

#### 垂直越权

A 用户权限高于 B 用户，B 用户越权操作 A 用户的权限的情况称为垂直越权。

admin 管理员用户创建用户的数据包：

![image-20260731103401609](/assets/images/20/image-20260731103401609.png)

普通用户 pikachu 的数据包，我们只需要他的 cookie 信息：

![image-20260731103442035](/assets/images/20/image-20260731103442035.png)

我们用 pikachu 的 cookie 信息去替换 admin 用户创建用户时的 cookie，发现 test1 创建成功，那么这里就存在垂直越权漏洞。

![image-20260731103707644](/assets/images/20/image-20260731103707644.png)

#### 未授权访问

在没有授权（登录的状态下），成功访问了只有授权（登录）才能看到的页面。
